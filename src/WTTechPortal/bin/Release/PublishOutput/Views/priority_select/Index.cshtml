@model IEnumerable<WTTechPortal.Models.priority_select>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.priorityname)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.priorityname)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.priorityid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.priorityid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.priorityid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
